const $CONST = {
  System: {
    UndefinedInteractionType: 'Type of Interaction can not be identified',
    AbstractMethodLacksImplementation:
      'This method is abstract and supposes the implementation',
  },
};

export default $CONST;
